// NOTE: In Tailwind v4 this legacy JS config extension is inert — shadow
// utilities (`shadow-soft`, `shadow-card`, `shadow-float`, ...) are defined
// via `@theme inline` in `src/app/style/globals.css`. Keys kept here that
// reference undefined `--shadow-*` vars previously compiled to no shadow.
export const boxShadow = {};