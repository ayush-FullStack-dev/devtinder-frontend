import Header from "@/components/shared/Header";

const VerifyLoginHeader = () => {
  return (
    <Header
      title="Verify It's You"
      description="Choose a secure method to continue signing into your account."
    />
  );
};

export default VerifyLoginHeader;
